name: Build check

on:
  pull_request:
    branches: ["main"]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: ESLint Action eslintrc
      run: eslint . --ext .js, .cjs
