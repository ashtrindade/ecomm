name: Build check

on:
  pull_request:
    branches: ["main"]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
    - name: Install Modules
      run: npm i
    - name: ESLint Action eslintrc
      run: eslint . --ext .js, .cjs

    - name: Run Jest
      uses: stefanoeb/jest-action@1.0.4
